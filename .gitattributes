* text=auto
# The script contains Cyrillic/CJK strings and must stay UTF-8 with BOM,
# otherwise Windows PowerShell 5.1 misreads it.
*.ps1 text eol=crlf
*.md  text
*.yml text
*.png binary
*.ico binary
